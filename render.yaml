services:
  - type: web
    name: meviplast-backend
    runtime: python
    buildCommand: pip install -r requirement.txt
    startCommand: gunicorn wsgi:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
